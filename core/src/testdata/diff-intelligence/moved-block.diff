diff --git a/src/move.ts b/src/move.ts
--- a/src/move.ts
+++ b/src/move.ts
@@ -1,7 +1,4 @@ function configureDashboard
-  const summaryPanel = createPanel('summary', metrics.total);
-  registerDashboardPanel(summaryPanel, { priority: 'high' });
-
   const detailPanel = createPanel('details', metrics.items.length);
   registerDashboardPanel(detailPanel, { priority: 'normal' });
 }
@@ -18,4 +15,7 @@ function configureSidebar
   const helpPanel = createPanel('help', docs.length);
   registerDashboardPanel(helpPanel, { priority: 'low' });
+
+  const summaryPanel = createPanel('summary', metrics.total);
+  registerDashboardPanel(summaryPanel, { priority: 'high' });
 }
