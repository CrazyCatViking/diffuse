name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.platform }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux-x64
            runner: ubuntu-latest
            artifact_pattern: app/dist/*.tar.gz
            unpacked_dir: dist/linux-unpacked
          - platform: mac-arm64
            runner: macos-latest
            artifact_pattern: app/dist/*.zip
            unpacked_dir: dist/mac-arm64
          - platform: win-x64
            runner: windows-latest
            artifact_pattern: app/dist/*.zip
            unpacked_dir: dist/win-unpacked

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Zig
        uses: mlugg/setup-zig@v2
        with:
          version: 0.16.0

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@1.90.0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Set up pnpm
        shell: bash
        run: |
          corepack enable
          corepack prepare pnpm@11.4.0 --activate

      - name: Build core
        working-directory: core
        run: zig build -Doptimize=ReleaseSafe

      - name: Test core
        working-directory: core
        run: zig build test

      - name: Install app dependencies
        working-directory: app
        run: pnpm install --frozen-lockfile

      - name: Build release Rust workspace and stage native artifacts
        run: cargo build --workspace --release --locked

      - name: Stage release native addon and Rust helper
        working-directory: app
        run: pnpm native:stage:release

      - name: Build unpacked app
        working-directory: app
        run: |
          pnpm build
          pnpm prepare:electron
          pnpm exec electron-builder --dir

      - name: Smoke unpacked app native resources
        working-directory: app
        run: pnpm smoke:native:packaged

      - name: Build app archive
        working-directory: app
        run: pnpm exec electron-builder --prepackaged "${{ matrix.unpacked_dir }}"

      - name: Upload release artifact
        uses: actions/upload-artifact@v4
        with:
          name: diffuse-${{ matrix.platform }}
          path: ${{ matrix.artifact_pattern }}
          if-no-files-found: error

  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-artifacts

      - name: Create release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ github.ref_name }}
        run: |
          files=$(find release-artifacts -type f | sort)
          gh release create "$TAG" $files \
            --repo "$GITHUB_REPOSITORY" \
            --title "Diffuse $TAG" \
            --notes "Prebuilt Diffuse release artifacts for Linux, macOS, and Windows."
