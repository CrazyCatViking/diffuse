diff --git a/src/move-edit.ts b/src/move-edit.ts
--- a/src/move-edit.ts
+++ b/src/move-edit.ts
@@ -2,8 +2,4 @@ function buildSummary
   const rows = collectRows(input);
-  const summaryPanel = createPanel('summary', rows.length);
-  summaryPanel.title = 'Summary';
-  registerDashboardPanel(summaryPanel, { priority: 'high' });
-
   return rows;
 }
@@ -20,4 +16,8 @@ function buildDashboard
   const chartPanel = createPanel('chart', rows.length);
   registerDashboardPanel(chartPanel, { priority: 'normal' });
+
+  const summaryPanel = createPanel('overview', rows.length);
+  summaryPanel.title = 'Overview';
+  registerDashboardPanel(summaryPanel, { priority: 'high' });
 }
